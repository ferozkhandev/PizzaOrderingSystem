logging:
  level:
    root: debug

spring:
  datasource:
    url: jdbc:postgresql://db:5432/productdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
